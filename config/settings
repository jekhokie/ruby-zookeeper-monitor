[global]
zk_ip   = 10.11.12.10   # IP of the ZooKeeper service host
zk_port = 2181          # Port for the ZooKeeper service
zk_path = /services     # Root node under which services subscribe/are monitored

[simulator]
num_services = 12       # Number of services you wish to simulate

[monitor]
